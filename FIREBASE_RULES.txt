# Firestore Security Rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Turnos: el cliente puede crear, admin puede gestionar
    match /turnos/{turnoId} {
      allow create: if true;
      allow read, update, delete: if request.auth != null && request.auth.token.admin == true;
    }

    // Clientes y subcolecciones
    match /clientes/{clienteId} {
      allow read, write: if request.auth != null && request.auth.token.admin == true;

      match /mascotas/{mascotaId} {
        allow read, write: if request.auth != null && request.auth.token.admin == true;

        match /historias/{historiaId} {
          allow read, write: if request.auth != null && request.auth.token.admin == true;
        }
      }
    }
  }
}

# Firebase Storage Rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}

# How to set admin custom claims:
# Run this in Firebase Functions or Firebase CLI:
# 
# const admin = require('firebase-admin');
# admin.auth().setCustomUserClaims('USER_UID', { admin: true });
